version: "3"

services:
  swagger-ui:
    image: charlesbases/swagger-ui:latest
    container_name: swagger-ui
    hostname: swagger_ui
    restart: always
    environment:
      TZ: Asia/Shanghai
    volumes:
      - /opt/swagger/doc:/swagger/api
    ports:
      - "18888:18080"

networks:
  default:
    name: swagger